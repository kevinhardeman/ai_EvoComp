startEvoComp()
{
	export LD_LIBRARY_PATH=~/AI_UvA/ai_EvoComp/Assignment
}

runEvoComp1()
{
	javac -cp contest.jar player40.java Elephant.java && java -jar testrun.jar -submission=player40 -evaluation=BentCigarFunction -seed=1
}

runEvoComp2()
{
	javac -cp contest.jar player40.java Elephant.java && java -jar testrun.jar -submission=player40 -evaluation=KatsuuraEvaluation -seed=1
}

runEvoComp3()
{
	javac -cp contest.jar player40.java Elephant.java && java -jar testrun.jar -submission=player40 -evaluation=SchaffersEvaluation -seed=1
}
